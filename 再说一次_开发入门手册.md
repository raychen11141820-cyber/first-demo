# 「再说一次」开发入门手册
## 从零到第一个可运行版本

> 适用人群：第一次开发网页，使用 Cursor AI 辅助  
> 目标：把「场景复刻 + 卡牌战斗」核心闭环跑通，部署到线上可访问

---

## 写在前面：你需要理解的三件事

**1. 代码放在哪里？**
```
你的电脑（Cursor 写代码）
       ↓ 推送
    GitHub（存代码，像云盘，免费）
       ↓ 自动部署
    Vercel（运行网站，用户能访问，免费）
```
每次你在 Cursor 改完代码，推送到 GitHub，Vercel 自动更新网站。

**2. 数据存在哪里？**
用 **Supabase**（免费云数据库），存用户的对战记录、结案报告等数据。

**3. 你的角色是什么？**
不需要自己写代码。你负责：
- 用自然语言告诉 Cursor AI 要做什么
- 看结果对不对
- 遇到报错截图告诉 AI 修

---

## 阶段零：准备工作（约 1 小时，只做一次）

### 0.1 注册四个账号

按顺序注册，都免费：

| 服务 | 地址 | 用途 |
|------|------|------|
| GitHub | https://github.com | 存代码 |
| Vercel | https://vercel.com | 部署网站，用 GitHub 账号登录 |
| Supabase | https://supabase.com | 数据库，用 GitHub 账号登录 |
| DeepSeek | https://platform.deepseek.com | AI 接口，充 10 元够用很久 |

### 0.2 安装三个软件

**按顺序安装，不要跳步：**

**① Node.js**
- 去 https://nodejs.org 下载 LTS 版本
- 一路点下一步安装
- 装完打开终端验证：
  ```
  node -v
  ```
  看到版本号（如 v20.x.x）说明成功

> Mac 用户：终端在「应用程序 → 实用工具 → 终端」  
> Windows 用户：按 Win 键搜索「PowerShell」

**② Git**
- 去 https://git-scm.com 下载
- 一路默认安装
- 装完验证：
  ```
  git --version
  ```

**③ Cursor**
- 去 https://cursor.com 下载
- 安装后用 GitHub 账号登录
- 登录后进入设置，把 AI 模型选为 claude-sonnet 或 gpt-4o（默认即可）

### 0.3 配置 Git 身份（只做一次）

打开终端，把下面两行的内容替换成你的信息后运行：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

---

## 阶段一：建项目（约 30 分钟）

### 1.1 在 GitHub 建仓库

1. 登录 GitHub，点右上角 **+** → **New repository**
2. Repository name 填：`saiagain`
3. 选 **Private**（私有，别人看不到）
4. 勾选 **Add a README file**
5. 点 **Create repository**
6. 复制页面上的仓库地址（格式：`https://github.com/你的用户名/saiagain.git`）

### 1.2 用 Cursor 建项目

打开 Cursor，点 **Open Folder**，选一个你放项目的位置（比如桌面或 Documents）。

按 **Ctrl+`**（反引号，Tab 键上面那个）打开终端，粘贴运行：

```bash
npx create-next-app@latest saiagain --typescript --tailwind --app --src-dir --no-git
```

运行过程中会问几个问题：
- Would you like to use ESLint? → **Yes**
- Would you like to use `src/` directory? → **Yes**（如果问）
- 其他全部回车默认

运行完后：
```bash
cd saiagain
```

### 1.3 安装依赖包

继续在终端运行：
```bash
npm install lucide-react
npm install ai @ai-sdk/openai
npm install @supabase/supabase-js
npm install html2canvas
```

### 1.4 连接 GitHub

```bash
git init
git add .
git commit -m "初始化项目"
git branch -M main
git remote add origin 你在1.1复制的仓库地址
git push -u origin main
```

推送完后刷新 GitHub 页面，能看到文件说明成功。

---

## 阶段二：让 Cursor AI 搭建项目骨架（约 1 小时）

> 从这里开始，你主要是在 Cursor 里对 AI 说话，不需要自己写代码。

### 2.1 打开 Cursor AI 对话

在 Cursor 里按 **Ctrl+Shift+I**（或点左侧聊天图标），打开 AI 对话面板。

确保模式是 **Agent**（在对话框左下角可以切换），Agent 模式下 AI 可以自动建文件。

### 2.2 第一个 AI 任务：建文件结构

把下面这段话**完整复制**发给 Cursor AI：

---

```
请帮我为这个 Next.js 14 项目建立完整的文件结构。项目名叫「再说一次」，
是一个情绪训练产品，用户可以选择预设场景，和 AI 扮演的「反派」对话，
用战术卡进行回击，最终完成结案报告。

请创建以下文件（如果已存在则跳过）：

src/types/index.ts          — 类型定义
src/lib/presets.ts          — 预设场景数据
src/lib/prompts.ts          — AI prompt 管理
src/lib/supabase.ts         — Supabase 客户端
src/app/page.tsx            — 首页（场景选择）
src/app/battle/page.tsx     — 战斗页
src/app/report/page.tsx     — 结案报告页
src/app/api/villain/route.ts    — 反派 AI 接口
src/app/api/advisor/route.ts    — 军师 AI 接口
src/app/api/judge/route.ts      — 评分判定接口
src/components/HpBar.tsx        — 血条组件
src/components/CardHand.tsx     — 战术卡手牌组件
src/components/BattleChat.tsx   — 对话气泡组件
src/components/AdvisorPanel.tsx — 军师面板组件
src/components/ReportCard.tsx   — 结案报告卡片组件

暂时每个文件写最基础的占位内容（export default function + 文件说明注释），
让项目能跑起来就行，不需要实现功能。
```

---

等 AI 建完，检查左侧文件树里这些文件都出现了。

### 2.3 第二个 AI 任务：写类型定义

发给 AI：

---

```
请帮我完善 src/types/index.ts，定义以下 TypeScript 类型：

1. TacticCard（战术卡）
   - id: string
   - name: string（如「举证压制」）
   - emoji: string
   - description: string（一句话说明这张牌的策略）
   - unlocked: boolean

2. ScenePreset（预设场景）
   - id: string
   - title: string（如「早会，王总当众批评你」）
   - villain: { name: string; role: string; traits: string[]; openingLine: string }
   - difficulty: '初级' | '中级' | '地狱'
   - tags: string[]

3. Message（对话消息）
   - id: string
   - role: 'user' | 'villain' | 'advisor'
   - content: string
   - score?: number（用户消息才有，0-10）
   - tacticUsed?: string（用户消息才有，使用的战术卡名）

4. JudgeResult（评分结果）
   - emotionScore: number（情绪强度，0-10）
   - logicScore: number（逻辑命中，0-10）
   - timingScore: number（时机，0-10）
   - specificityScore: number（具体性，0-10）
   - totalScore: number（加权总分，0-10）
   - tacticMatch: boolean（是否匹配所选战术）
   - moveName: string（招式名，如「釜底抽薪」）
   - hpDamage: number（对反派造成的血量伤害，0-20）

5. BattleState（战斗状态）
   - scene: ScenePreset
   - messages: Message[]
   - villainHp: number（0-100）
   - selectedTactic: TacticCard | null
   - isVillainTyping: boolean
   - isAdvisorTyping: boolean
   - phase: 'selecting' | 'battling' | 'finished'
```

---

### 2.4 第三个 AI 任务：写预设场景数据

发给 AI：

---

```
请帮我完善 src/lib/presets.ts。

写两个预设场景数据：

场景一：王总早会
- 王总是直属上级，擅长道德绑架和阴阳怪气
- 场景是周一早会，王总当众拿你的周报发难
- 开场白：「小X啊，你这个数据……唉，我不说了，你自己看看，这叫什么东西？」
- 难度：初级

场景二：同事小李甩锅
- 小李是同级同事，擅长甩锅和背刺
- 场景是季度复盘会议，项目出了问题，小李在领导面前把责任推给你
- 开场白：「王总，这块其实当时我跟小X说过的，让他注意一下，他可能当时比较忙……」
- 难度：中级

同时写出四张基础战术卡：
- 🪞 原话奉还：用对方的逻辑反推回去
- 📋 举证压制：抛出具体事实和细节
- 🌊 冷处理：不接招，降温转移话题
- 💣 直接点破：说出对方话术的本质

数据格式用 TypeScript，export const PRESET_SCENES 和 export const BASIC_CARDS。
```

---

### 2.5 第四个 AI 任务：写 AI 接口

发给 AI：

---

```
请帮我完善以下三个 API 路由，使用 DeepSeek API（兼容 OpenAI 格式，
base URL 是 https://api.deepseek.com/v1，key 从环境变量 DEEPSEEK_API_KEY 读取），
使用流式输出（streaming）：

1. src/app/api/villain/route.ts
   接收：{ messages, scene, villainHp, systemPrompt }
   功能：扮演反派，根据当前血量状态调整语气
   - 血量 100-70：强势笃定，偶尔轻蔑
   - 血量 70-40：开始心虚，偶尔停顿找补
   - 血量 40-10：频繁找补，开始用模糊措辞
   - 血量 10-0：结巴，自相矛盾，垂死反扑
   返回：流式文本

2. src/app/api/advisor/route.ts
   接收：{ villainLastMessage, conversationHistory, selectedTactic }
   功能：军师分析，给出本轮战术提示
   规则：只给方向，不给具体台词，语气要短、快、站在用户这边
   返回：流式文本，格式为「他在用【XX话术】\n你可以往XX方向打」

3. src/app/api/judge/route.ts
   接收：{ userMessage, villainMessage, selectedTactic, conversationHistory }
   功能：评判用户这句话的质量，返回 JSON
   返回：JudgeResult 格式的 JSON（不要流式，直接返回）
   评分维度：情绪强度/逻辑命中/时机/具体性，各 0-10 分
   招式名要有创意，根据战术和内容生成，如「釜底抽薪」「铁证如山」「冷眼旁观」

注意：judge 接口要在 prompt 里明确要求 AI 只返回 JSON，不要其他内容。
```

---

### 2.6 第五个 AI 任务：搭战斗页面

发给 AI：

---

```
请帮我实现 src/app/battle/page.tsx，这是核心战斗页面。

页面接收 URL 参数 sceneId（如 /battle?sceneId=boss-meeting），
根据 sceneId 从 presets 里找到对应场景。

页面布局（移动端优先）：
- 顶部：反派名字 + 血条（HpBar 组件）
- 中间：对话区域（BattleChat 组件），可滚动
- 侧边/底部折叠：军师面板（AdvisorPanel 组件）
- 底部：战术卡手牌（CardHand 组件）+ 输入框 + 发送按钮

战斗流程：
1. 页面加载后，反派自动说开场白
2. 用户先从 CardHand 选一张战术卡（选中高亮）
3. 用户在输入框输入内容，点发送
4. 调用 /api/judge 获取评分，更新血条
5. 调用 /api/villain 获取反派回应（流式，打字机效果）
6. 反派说完后，调用 /api/advisor 获取军师提示（流式）
7. 血量归零时跳转 /report

状态用 useReducer 管理，类型用 BattleState。
样式用 Tailwind，风格简洁深色系。
```

---

## 阶段三：配置环境变量和数据库（约 30 分钟）

### 3.1 配置本地环境变量

在项目根目录找到 `.env.local` 文件（没有就新建），填入：

```
DEEPSEEK_API_KEY=你的DeepSeek_key

NEXT_PUBLIC_SUPABASE_URL=后面从Supabase复制
NEXT_PUBLIC_SUPABASE_ANON_KEY=后面从Supabase复制
```

### 3.2 建 Supabase 数据库

1. 登录 https://supabase.com，点 **New Project**
2. 填项目名 `saiagain`，设数据库密码（记住它），选离你近的区域（Singapore 或 Tokyo）
3. 等待创建完成（约 2 分钟）
4. 进入项目 → **Settings** → **API**，复制：
   - `Project URL` → 填入 `.env.local` 的 `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` key → 填入 `NEXT_PUBLIC_SUPABASE_ANON_KEY`

5. 进入 **SQL Editor**，运行这段 SQL 建表：

```sql
create table battle_records (
  id uuid default gen_random_uuid() primary key,
  created_at timestamp with time zone default now(),
  scene_id text not null,
  scene_title text not null,
  messages jsonb not null,
  final_hp integer not null,
  total_score integer not null,
  best_comeback text,
  verdict text
);
```

### 3.3 让 AI 完善 Supabase 客户端

发给 Cursor AI：

---

```
请帮我完善 src/lib/supabase.ts，创建 Supabase 客户端，
并写一个 saveBattleRecord 函数，
接收战斗结束时的数据，保存到 battle_records 表。
从环境变量 NEXT_PUBLIC_SUPABASE_URL 和 NEXT_PUBLIC_SUPABASE_ANON_KEY 读取配置。
```

---

## 阶段四：本地跑起来（约 15 分钟）

### 4.1 启动开发服务器

终端运行：
```bash
npm run dev
```

浏览器打开 http://localhost:3000

### 4.2 排错方法

遇到红色报错不要慌，三步处理：

1. 复制终端里的报错信息
2. 发给 Cursor AI：「我运行 npm run dev 遇到这个报错，帮我修复：[粘贴报错]」
3. 等 AI 修复，再试

**常见报错：**

| 报错关键词 | 原因 | 让 AI 说 |
|-----------|------|---------|
| Cannot find module | 少装了包 | 帮我安装缺少的依赖 |
| Type error | 类型不匹配 | 帮我修复这个 TypeScript 类型错误 |
| DEEPSEEK_API_KEY | key 没配置 | 检查 .env.local 是否正确 |
| fetch failed | 网络问题或 API 地址错误 | 帮我检查 API 调用配置 |

---

## 阶段五：部署上线（约 20 分钟）

### 5.1 推送代码到 GitHub

每次改完代码想更新网站，运行：
```bash
git add .
git commit -m "描述这次改了什么"
git push
```

### 5.2 连接 Vercel

1. 登录 https://vercel.com，点 **Add New Project**
2. 选 **Import Git Repository**，找到 `saiagain`，点 Import
3. 框架会自动识别为 Next.js，不用改配置
4. 点 **Deploy**，等待约 2 分钟

### 5.3 在 Vercel 配置环境变量

部署完成后：
1. 进入项目 → **Settings** → **Environment Variables**
2. 添加三个变量（和 .env.local 里一样）：
   - `DEEPSEEK_API_KEY`
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. 添加完后点 **Redeploy** 重新部署一次

完成后 Vercel 会给你一个网址（如 `saiagain.vercel.app`），可以直接访问了。

**之后每次推送代码到 GitHub，Vercel 自动更新，不需要手动操作。**

---

## 阶段六：首页和结案报告（跑通闭环）

等战斗页跑通后，发给 Cursor AI：

**首页：**
```
请帮我实现 src/app/page.tsx 首页。
展示可选场景列表，每个场景显示标题、难度、标签。
点击场景跳转到 /battle?sceneId=对应id。
风格深色系，有点游戏感，但不花哨。
```

**结案报告页：**
```
请帮我实现 src/app/report/page.tsx。
从 URL 参数或 localStorage 读取战斗结果数据，
调用 DeepSeek API 生成结案报告（包含判决、识别的话术、本场最强一击、评分、段位）。
页面要像游戏结算界面，有截图保存按钮（用 html2canvas）。
保存记录到 Supabase 的 battle_records 表。
```

---

## 日常开发节奏

跑通基础版本后，每次新增功能的流程：

```
1. 想清楚要加什么
       ↓
2. 在 Cursor AI 里描述清楚，让它写代码
       ↓
3. npm run dev 本地看效果
       ↓
4. 有问题截图发给 AI 修
       ↓
5. 没问题：git add . → git commit → git push
       ↓
6. Vercel 自动更新线上版本
```

---

## Cursor AI 使用技巧

**描述任务时要具体：**
- ❌ 「帮我做战斗页面」
- ✅ 「帮我在战斗页面底部加一行血条，红色，根据 villainHp 的值（0-100）显示宽度，扣血时有 0.3s 的过渡动画」

**遇到 AI 写出来不对时：**
- 「这个实现有问题，[描述哪里不对]，请重新写」
- 「保留现有逻辑，只修改[具体部分]」

**让 AI 一次做一件事：**
- 不要一次让它改五个文件，容易乱
- 一个功能确认没问题了，再让它做下一个

---

*手册版本 1.0 · 配合「再说一次」产品设计文档 v2.1 使用*
